// active hamburger menu
let menuIcon = document.querySelector(".menu-icon"); //for select the class

menuIcon.addEventListener("click", () => { //for clicking
    menuIcon.classList.toggle("active"); //for response
})